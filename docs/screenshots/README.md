Screenshots referenced by the project README. Filenames are fixed — replacing an
image means overwriting the file, not adding a new one.

  dashboard.png       fleet view: device cards + alert feed
  map.png             Map tab: GPS track on OpenStreetMap
  chart-sound.png     Sound tab: dB over time
  chart-motion.png    Motion tab: accelerometer, with a shake spike
  chart-battery.png   Battery tab: drain over time
  login.png           sign-in page with the demo button
